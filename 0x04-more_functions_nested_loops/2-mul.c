#include <stdlib.h>
#include "main.h"

/**
 * mul - multiplies two arguments
 * @x: argument 1
 * @y: argument 2
 * Return: Returns result
 */

int mul(int x, int y)
{
	int result = x * y;

	return (result);
	return (0);
}
